# TypeScript Torture Fixture

Holds TypeScript-focused samples that mimic tricky syntax patterns (e.g., `satisfies`, `as const` indexing, template literal inference). The harness will parse and typecheck these snippets to catch regressions in generated output.
