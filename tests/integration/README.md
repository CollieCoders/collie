# Integration Tests

New user-perspective harnesses now live in `packages/collie-tests`. This directory is retained as a pointer for legacy suites and can be expanded later if we migrate scenarios back into the root `tests/` folder.
